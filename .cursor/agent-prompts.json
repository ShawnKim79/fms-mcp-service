[
  {
    "description": "검색 쿼리와 가장 관련 있는 코드 스니펫을 코드베이스에서 찾습니다. 특별한 이유가 없다면 사용자의 표현을 그대로 재사용하세요. 필요 시 단일 디렉터리로 범위를 제한할 수 있습니다.",
    "name": "codebase_search",
    "parameters": {
      "properties": {
        "explanation": { "description": "이 검색이 목표 달성에 도움이 되는 이유.", "type": "string" },
        "query": { "description": "관련 코드를 찾기 위한 완전한 검색 질문.", "type": "string" },
        "target_directories": { "description": "스코프를 제한할 디렉터리 경로 접두사(단일 디렉터리만, 글롭 패턴 금지). 전체 레포는 [].", "items": { "type": "string" }, "type": "array" },
        "search_only_prs": { "description": "true이면 PR만 검색하고 코드 결과는 반환하지 않습니다.", "type": "boolean" }
      },
      "required": ["explanation", "query", "target_directories"],
      "type": "object"
    }
  },
  {
    "description": "워크스페이스의 파일을 읽습니다. 파일이 크면 offset/limit를 제공하세요.",
    "name": "read_file",
    "parameters": {
      "properties": {
        "target_file": { "description": "파일 경로(상대 또는 절대).", "type": "string" },
        "offset": { "description": "읽기 시작 라인 번호(1부터).", "type": "integer" },
        "limit": { "description": "읽을 라인 수. 전체 읽기는 생략(절약).", "type": "integer" }
      },
      "required": ["target_file"],
      "type": "object"
    }
  },
  {
    "description": "사용자 대신 실행할 터미널 명령을 제안합니다. 실행 전 사용자 승인이 필요합니다.",
    "name": "run_terminal_cmd",
    "parameters": {
      "properties": {
        "command": { "description": "실행할 터미널 명령", "type": "string" },
        "is_background": { "description": "백그라운드 실행 여부", "type": "boolean" },
        "explanation": { "description": "이 명령이 목표 달성에 도움이 되는 이유.", "type": "string" }
      },
      "required": ["command", "is_background"],
      "type": "object"
    }
  },
  {
    "description": "디렉터리 내용을 나열합니다.",
    "name": "list_dir",
    "parameters": {
      "properties": {
        "target_directory": { "description": "목록을 볼 경로.", "type": "string" },
        "ignore_globs": { "description": "무시할 글롭 패턴 배열.", "type": "array", "items": { "type": "string" } },
        "explanation": { "description": "이 목록 보기가 목표 달성에 도움이 되는 이유.", "type": "string" }
      },
      "required": ["target_directory"],
      "type": "object"
    }
  },
  {
    "description": "정확한 문자열/정규식 검색을 위한 ripgrep 기반 강력한 검색 도구.",
    "name": "grep",
    "parameters": {
      "properties": {
        "pattern": { "description": "검색할 정규식 패턴.", "type": "string" },
        "path": { "description": "검색할 파일 또는 디렉터리.", "type": "string" },
        "glob": { "description": "포함할 글롭(예: '*.py').", "type": "string" },
        "output_mode": { "description": "출력 형식.", "type": "string", "enum": ["content", "files_with_matches", "count"] },
        "-B": { "description": "일치 전 컨텍스트 라인 수.", "type": "number" },
        "-A": { "description": "일치 후 컨텍스트 라인 수.", "type": "number" },
        "-C": { "description": "전후 컨텍스트 라인 수.", "type": "number" },
        "-i": { "description": "대소문자 구분 없음.", "type": "boolean" },
        "type": { "description": "파일 유형 필터(예: py, js).", "type": "string" },
        "head_limit": { "description": "출력 라인/엔트리 제한.", "type": "number" },
        "multiline": { "description": "멀티라인 정규식 모드 활성화.", "type": "boolean" }
      },
      "required": ["pattern"],
      "type": "object"
    }
  },
  {
    "description": "파일을 생성/편집합니다. 최소·정밀한 에디트를 제공하고, 변경되지 않은 부분은 // ... existing code ... 로 생략하세요.",
    "name": "edit_file",
    "parameters": {
      "properties": {
        "target_file": { "description": "수정할 대상 파일.", "type": "string" },
        "instructions": { "description": "에디트를 설명하는 한 문장 지시.", "type": "string" },
        "code_edit": { "description": "정확히 바꿀 코드만 기재; 간격은 // ... existing code ... 사용.", "type": "string" }
      },
      "required": ["target_file", "instructions", "code_edit"],
      "type": "object"
    }
  },
  {
    "description": "글롭 패턴으로 파일을 검색합니다.",
    "name": "glob_file_search",
    "parameters": {
      "properties": {
        "target_directory": { "description": "검색할 디렉터리.", "type": "string" },
        "glob_pattern": { "description": "매칭할 글롭 패턴.", "type": "string" }
      },
      "required": ["glob_pattern"],
      "type": "object"
    }
  },
  {
    "description": "지정된 경로의 파일을 삭제합니다.",
    "name": "delete_file",
    "parameters": {
      "properties": {
        "target_file": { "description": "삭제할 파일 경로.", "type": "string" },
        "explanation": { "description": "삭제가 목표 달성에 도움이 되는 이유.", "type": "string" }
      },
      "required": ["target_file"],
      "type": "object"
    }
  },
  {
    "description": "실시간 정보를 웹에서 검색합니다.",
    "name": "web_search",
    "parameters": {
      "properties": {
        "search_term": { "description": "검색 키워드.", "type": "string" },
        "explanation": { "description": "이 검색이 목표 달성에 도움이 되는 이유.", "type": "string" }
      },
      "required": ["search_term"],
      "type": "object"
    }
  },
  {
    "description": "Mermaid 다이어그램을 생성합니다.",
    "name": "create_diagram",
    "parameters": {
      "properties": {
        "content": { "description": "Mermaid DSL 원문.", "type": "string" }
      },
      "required": ["content"],
      "type": "object"
    }
  },
  {
    "description": "주피터 노트북 셀을 편집합니다. 충분한 고유 컨텍스트와 함께 정확한 old_string/new_string을 제공합니다.",
    "name": "edit_notebook",
    "parameters": {
      "properties": {
        "target_notebook": { "description": "노트북 파일 경로.", "type": "string" },
        "cell_idx": { "description": "0부터 시작하는 셀 인덱스.", "type": "number" },
        "is_new_cell": { "description": "새 셀 생성 여부.", "type": "boolean" },
        "cell_language": { "description": "셀의 언어.", "type": "string", "enum": ["python", "markdown", "javascript", "typescript", "r", "sql", "shell", "raw", "other"] },
        "old_string": { "description": "충분한 고유 컨텍스트를 포함한 정확한 교체 대상 텍스트.", "type": "string" },
        "new_string": { "description": "대체 또는 새 셀 내용.", "type": "string" }
      },
      "required": ["target_notebook", "cell_idx", "is_new_cell", "cell_language", "old_string", "new_string"],
      "type": "object"
    }
  }
]